package ar.edu.unlu.poo.uno.model;

import org.junit.Test;

import java.util.ArrayList;
import java.util.Arrays;

import static org.junit.Assert.*;

public class MezcladorTest {
   /* @Test
    public void cantCartaslleno(){
        Mezclador mezcla = new Mezclador();
        ArrayList<Integer> comparacion1 = new ArrayList<>();
        comparacion1.addAll(Arrays.asList(1, 2, 2, 2, 2, 2, 2, 2, 2, 2, 2, 2, 2));
        ArrayList<Integer> comparacion2 = mezcla.getCantCartas();

        Integer[] expectedArray = comparacion1.toArray(new Integer[0]);
        Integer[] actualArray = comparacion2.toArray(new Integer[0]);

        assertArrayEquals(expectedArray, actualArray);
    }
    @Test
    public void cantCartas(){
        Mezclador mezcla = new Mezclador();
        ArrayList<Carta> mazo = new ArrayList<>();
        mazo = mezcla.mezclar();
        int expect = 108;
        int actual = mazo.size();
        assertEquals(expect, actual);
    }*/
}