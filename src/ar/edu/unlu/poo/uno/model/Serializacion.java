package ar.edu.unlu.poo.uno.model;

import java.io.Serializable;

public class Serializacion implements Serializable {
    /*ArrayList<Libro> libros = new ArrayList<>();
        libros.add(new Libro("Mi libro", "Fede", 1));
        libros.add(new Libro("Mi segundo libro", "Fede", 2));

    // Escribir objetos
        try {
        var outputStream = new FileOutputStream("libros.dat");
        var objectOutputStream = new ObjectOutputStream(outputStream);
        objectOutputStream.writeObject(libros);
    } catch (FileNotFoundException e) {
        throw new RuntimeException(e);
    } catch (IOException e) {
        throw new RuntimeException(e);
    }

    // Leer objetos
    ArrayList<Libro> librosLeidos;
        try {
        var inputStream = new FileInputStream("libros.dat");
        var objectInputStream = new ObjectInputStream(inputStream);
        librosLeidos = (ArrayList<Libro>) objectInputStream.readObject();
    } catch (FileNotFoundException e) {
        throw new RuntimeException(e);
    } catch (IOException e) {
        throw new RuntimeException(e);
    } catch (ClassNotFoundException e) {
        throw new RuntimeException(e);
    }

        for (Libro libro : libros) {
        System.out.println(libro + " " + libro.getTitulo());
    }

        for (Libro libro : librosLeidos) {
        System.out.println(libro + " " + libro.getTitulo());
    }*/

}
